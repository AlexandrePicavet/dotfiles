(defwidget power-button [monitor]
	(button
		:class "button_power"
		:onclick "eww open --toggle powermenu --id 'powermenu-${monitor}' --arg 'monitor=${monitor}'"
		(label
			:yalign "0.5"
			:markup '<span line_height="0.6" font_size="x-large"></span>'
		)
	)
)

